import * as PlayerUtil from "shared/player_utils"

/*
const playerService = game.GetService("Players")

let longswordIdleAnimation = "rbxassetid://6717931698"

playerService.PlayerAdded.Connect(player => {
  player.CharacterAppearanceLoaded.Connect(character => {
    let humanoid = character.WaitForChild("Humanoid")
    let animateScript = character.WaitForChild("Animate") as LocalScript
    let animation1 = animateScript.WaitForChild("toolnone").WaitForChild("ToolNoneAnim") as Animation
    animation1.AnimationId = longswordIdleAnimation;
  })
})
*/